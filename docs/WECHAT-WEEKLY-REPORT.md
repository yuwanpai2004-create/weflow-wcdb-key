# 微信周报脚本

这个脚本用于生成《用户运营数据周报》中微信相关的三行可复制数据：

- `加好友周报`
- `社群周报`
- `群友互动周报`

## 使用前准备

1. 启动 WeFlow，并连接微信数据库。
2. 在 WeFlow 设置中开启 `HTTP API 服务`。
3. 如果还要统计非完善用户，打开通讯录页并刷新一次，让 WeFlow 建立“好友首次出现”追踪基线。
4. 生成脚本配置：

```bash
npm run weekly:wechat:init
```

然后编辑：

```text
scripts/wechat-weekly-report.config.json
```

需要填写：

- `accounts[].apiBaseUrl`：WeFlow API 地址。一台电脑切换微信账号时，两个账号通常都填同一个地址，例如 `http://127.0.0.1:5031`。
- `accounts[].accessToken`：如果 API 服务开启了 Token，就填这里。
- `accounts[].firstSeenCacheScope`：多个账号时必须区分，通常直接填账号名，用于区分各账号的好友首次出现缓存。
- `groupStatsAccountName`：只用哪个账号统计社群进群和群友互动，通常填负责维护社群的那个微信账号名。
- `completedMessageKeywords`：识别完善用户的固定话术。默认是 `感谢您关注天天开源软件，现在拉您进专属技术交流群，有问题欢迎随时咨询~`。
- `staffNamePrefixes`：群友互动统计中需要排除的工作人员姓名开头。
- `staffExactNames`：群友互动统计中需要排除的工作人员姓名关键字，例如配置 `罗文` 也会排除 `天天开源商务-罗文`。
- `groups[].chatroomIds`：一个周报栏目对应的一个或多个真实微信群 ID，格式为 `xxx@chatroom`。

群 ID 可以在 WeFlow 通讯录里搜索群名，或用本地 API 查询联系人列表后找到对应 `username`。

例如“医疗技术群”有三个真实微信群时：

```json
{
  "label": "医疗技术群",
  "chatroomIds": [
    "医疗技术群1的ID@chatroom",
    "医疗技术群2的ID@chatroom",
    "医疗技术群3的ID@chatroom"
  ],
  "countForFriendJoin": true
}
```

同一个栏目里的多个真实群会自动汇总。进群人次按所有真实群相加；群友互动人数按用户去重后再排除工作人员。

## 生成周报

一台电脑同一时间只能登录一个微信时，按下面流程操作。

先登录第一个微信账号，确保 WeFlow 已连接并开启 API，然后采集该账号：

```bash
npm run weekly:wechat -- --account 账号1
```

再退出微信，登录第二个微信账号，确保 WeFlow 重新连接到这个账号，然后采集第二个账号：

```bash
npm run weekly:wechat -- --account 账号2
```

脚本会把两个账号的好友统计缓存到本地，并自动合并成一份总表输出。只要配置了 `groupStatsAccountName`，社群周报和群友互动周报只会使用指定账号的数据，不会重复统计。

指定某个周日作为周报日期：

```bash
npm run weekly:wechat -- --account 账号1 --week-end 2026-06-07
```

输出文件在：

```text
outputs/wechat-weekly-report-YYYY-MM-DD.tsv
```

每个 sheet 会输出与总表一致的表头和一行数据。实际填写时，可以只复制数据行粘贴到总表对应 sheet；需要核对列时，也可以连同表头一起复制到临时表里查看。

## 统计口径

### 加好友周报

- 统计周期：周一 00:00:00 到周日 23:59:59。
- 默认运行时统计上周。
- 完善用户：本周私聊里，你发出过 `completedMessageKeywords` 中任意一句固定话术的好友。新版 HTTP API 会优先全局搜索这句消息；如果当前运行的是旧 API，会自动退回逐个好友会话扫描。
- 非完善用户：如果有好友首次出现缓存，则用“本周新增好友 - 完善用户”作为兜底统计；没有缓存时不会凭空估算。
- 技术群进群人数：本周识别到的新加好友里，有多少人进入配置中 `countForFriendJoin=true` 的群，按人去重。
- 两个账号的好友统计会自动合并到同一行，不会生成两张表。

### 社群周报

- 统计每个群本周有多少人进群。
- 这里按群统计人次：同一个人进了多个群，会在多个群各计一次。
- 一条邀请消息里有多个人，会按多个人计数。
- 只统计系统消息中真正的被邀请人或扫码入群人，不会把邀请人、二维码分享人计为新人。
- 只统计配置项 `groupStatsAccountName` 指定账号中的群聊数据。

### 群友互动周报

- 统计每个群本周有多少个用户发言。
- 同一个用户一周内多次发言，只计 1 人。
- 同一个周报栏目对应多个真实群时，同一个用户跨群发言仍只计 1 人。
- 系统消息不计入发言。
- 默认排除姓名以 `天天开源顾问`、`天天开源助理`、`天天开源产品` 开头的工作人员，以及 `夏灵东`、`罗文`。

## 注意

完善用户通过本周固定话术识别，不依赖好友快照。

非完善用户仍需要好友首次出现缓存作为兜底。如果周一才第一次建立基线，脚本无法倒推出上周新增好友的真实添加时间；这时会给出提示，非完善用户可能为 0。后续只要保持每周前刷新过通讯录，非完善用户就可以正常统计。

群聊统计以微信数据库里的群系统消息和发言消息为准。人工周报如果还会排除广告用户、退群用户或手工调整人数，可能与脚本的原始消息口径不同，需要在总表中继续人工调整对应列。

微信数据库密钥通常按微信账号区分保存，不应假设两个账号使用同一个密钥。这个脚本通过 WeFlow 当前连接账号的 HTTP API 采集数据，不会读取或打印密钥。
